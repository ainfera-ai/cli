---
description: Install the CLI in development mode for testing
---

Install the CLI package in editable mode so changes are reflected immediately.

```bash
pip install -e ".[dev]"
```

Verify it works:

```bash
ainfera --version
ainfera --help
ainfera health
```
